query GetUserProgress($userId: ID!) {
  getUserProgress(userId: $userId) {
    questionId
    answer
    isCorrect
    timeDuration
    timeAnswer
    userName
    completed
    explanation
    score
    success
  }
}
