query GetQuestions($bookId: ID, $chapterId: ID) {
  getQuestionsForBook(bookId: $bookId, chapterId: $chapterId) {
    id
    bookId
    chapterId
    question
    answer
    option {
      options
      explanation
    }
    createdAt
    updatedAt
  }
}
